# Сводка изменений: Firebase и расширенная админ-панель

## ✅ Выполнено

### Backend изменения

1. **Firebase Admin SDK**
   - Добавлен `firebase-admin` в зависимости
   - Создан `server/config/firebase.js` для инициализации Firebase
   - Поддержка сервисного аккаунта через JSON файл или переменные окружения

2. **Модель User обновлена** (`server/models/User.js`)
   - Добавлено поле `firebaseUid` для связи с Firebase пользователями
   - Добавлено поле `authProvider` (local, google, firebase)
   - Добавлено поле `lastLogin` для отслеживания активности
   - Пароль теперь опциональный (для Firebase пользователей)
   - Hooks обновлены для работы с разными провайдерами

3. **Google аутентификация** (`server/routes/auth.js`)
   - Добавлен маршрут `POST /api/auth/google` для входа через Google
   - Верификация Firebase ID токена на сервере
   - Автоматическое создание/обновление пользователя
   - Обновление `lastLogin` при входе

4. **Расширенная админ-панель API** (`server/routes/admin.js`)
   - `GET /api/admin/stats` - добавлена статистика активных пользователей
   - `GET /api/admin/users` - список всех пользователей (пагинация, фильтры)
   - `GET /api/admin/cargos` - список всех грузов (пагинация, фильтры)
   - `DELETE /api/admin/cargos/:id` - удаление груза (модерация)
   - `PUT /api/admin/cargos/:id` - редактирование груза (модерация)

5. **Документация**
   - Создан `FIREBASE_SETUP.md` с инструкциями по настройке Firebase
   - Обновлен `.gitignore` для исключения Firebase конфигурационных файлов
   - Обновлен `env.example` с переменными Firebase

## ⚠️ Требуется доработка

### Frontend изменения

1. **Расширенная админ-панель** (`public/app.js`)
   - Необходимо обновить функцию `loadAdminPanel()` для:
     - Добавления вкладок (Статистика, Пользователи, Грузы, Водители)
     - Отображения статистики активных пользователей
     - Списка всех пользователей
     - Списка всех грузов с возможностью модерации
     - Функций для удаления/редактирования грузов

2. **Google Sign-In на фронтенде**
   - Требуется настройка Firebase проекта
   - Добавление Firebase JS SDK или Google OAuth библиотеки
   - Добавление кнопки "Войти через Google" на страницах логина/регистрации
   - Интеграция с API endpoint `/api/auth/google`

## 📝 Инструкции

### Настройка Firebase

1. Создайте Firebase проект в [Firebase Console](https://console.firebase.google.com/)
2. Включите Google Authentication
3. Получите сервисный аккаунт (JSON файл)
4. Добавьте конфигурацию в `.env` (см. `FIREBASE_SETUP.md`)

### Обновление базы данных

После обновления модели User необходимо синхронизировать базу данных:

```bash
# База данных будет обновлена автоматически при следующем запуске
# Но для безопасности лучше создать backup:
cp database.sqlite database.sqlite.backup

# Затем запустить сервер
npm start
```

Sequelize автоматически добавит новые поля при запуске (если используется `alter: true`).

### Для разработки (с пересозданием таблиц)

⚠️ **ВНИМАНИЕ:** Это удалит все данные!

```bash
# Измените в server/models/index.js:
# await sequelize.sync({ force: true });

# Затем запустите:
npm start
npm run create-admin
npm run create-mocks
```

## 🔄 Следующие шаги

1. Настройте Firebase проект (см. `FIREBASE_SETUP.md`)
2. Обновите функцию `loadAdminPanel()` на фронтенде
3. Добавьте Google Sign-In кнопки на фронтенде
4. Протестируйте все функции

## 📚 API Endpoints (новые/обновленные)

### Аутентификация
- `POST /api/auth/google` - Вход через Google (Firebase)

### Админ панель
- `GET /api/admin/stats` - Статистика (включая активных пользователей)
- `GET /api/admin/users` - Список пользователей (пагинация, фильтры)
- `GET /api/admin/cargos` - Список всех грузов (пагинация, фильтры)
- `DELETE /api/admin/cargos/:id` - Удаление груза
- `PUT /api/admin/cargos/:id` - Редактирование груза

## 🔒 Безопасность

- ⚠️ Не коммитьте `firebase-service-account.json` в Git
- ✅ Используйте переменные окружения в продакшене
- ✅ Храните приватные ключи безопасно
- ✅ Обновите `.env` файл с реальными значениями для продакшена
