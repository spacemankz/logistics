# SHIPLY.KZ - Логистическая Платформа Казахстана

Одностраничное веб-приложение для логистики с авторизацией, платной регистрацией и системой управления грузами и водителями.

## Возможности

- **Авторизация и регистрация** - система аутентификации с JWT токенами
- **Email верификация** - подтверждение email через OTP коды
- **Активация аккаунта** - простая активация для тестирования (без реальных платежей)
- **Профиль грузоотправителя** - создание и управление заявками на перевозку грузов
- **Профиль водителя** - заполнение профиля с подтверждением администратором
- **Расширенная админ-панель** - управление пользователями, грузами, модерация, статистика ⭐ НОВОЕ
- **Мокапы данных** - тестовые данные для просмотра проекта

## Особенности

- **Казахстан** - все грузы с городами Казахстана
- **Валюта в тенге (₸)** - все цены в казахстанских тенге
- **Расширенная информация о грузах:**
  - Тип груза (Контейнер, Паллеты, Насыпной, Жидкость, Хрупкий, Скоропортящийся)
  - Тип машины (Открытый/Закрытый тент)
  - Вес в кг и тоннах
  - Объем в м³
  - Общая стоимость и стоимость за 1 км
  - Расстояние в км
  - Комментарий к заказу

## Технологии

### Backend
- **Node.js** - JavaScript runtime
- **Express.js** (v4.18.2) - веб-фреймворк для Node.js
- **Sequelize** (v6.32.1) - ORM для работы с базами данных
- **SQLite3** (v5.1.6) - встроенная база данных (не требует отдельного сервера)

### Frontend
- **Vanilla JavaScript** (ES6+) - без фреймворков
- **HTML5** - структура приложения
- **CSS3** - стилизация (responsive design, mobile-first)
- **SPA (Single Page Application)** - одностраничное приложение

### Безопасность и аутентификация
- **JWT (jsonwebtoken)** (v9.0.2) - токены для аутентификации
- **bcryptjs** (v2.4.3) - хеширование паролей
- **express-validator** (v7.0.1) - валидация данных

### Инструменты и утилиты
- **dotenv** (v16.3.1) - управление переменными окружения
- **nodemon** (v3.0.1) - автоматический перезапуск при разработке

### Архитектура
- **RESTful API** - архитектура API
- **MVC Pattern** - структура кода (Models, Routes, Middleware)
- **Middleware Pattern** - промежуточная обработка запросов

### Развертывание (для продакшена)
- **PM2** - процесс-менеджер для Node.js
- **Nginx** - reverse proxy и веб-сервер (опционально)
- **Let's Encrypt** - бесплатный SSL сертификат (опционально)
- **UFW** - файрвол (опционально)

## Установка и запуск

### Развертывание на VPS сервере

📖 **[Подробное руководство по развертыванию на VPS](VPS_DEPLOYMENT.md)** - пошаговая инструкция для развертывания на сервере с Ubuntu/Debian, включая настройку PM2, Nginx, SSL и firewall.

### Требования
- Node.js (v14 или выше)
- **Ничего больше!** SQLite работает из коробки

### Быстрый старт

1. **Установите зависимости:**
```bash
npm install
```

2. **Создайте файл `.env`** (опционально, есть значения по умолчанию):
```env
PORT=3000
NODE_ENV=development
DB_PATH=./database.sqlite
JWT_SECRET=your-secret-key
JWT_EXPIRE=7d
REGISTRATION_FEE=1000
ADMIN_EMAIL=admin@logistics.com
ADMIN_PASSWORD=admin123
```

3. **Создайте администратора:**
```bash
npm run create-admin
```

4. **Создайте тестовые данные (мокапы):**
```bash
npm run create-mocks
```

5. **Запустите приложение:**
```bash
npm start
```

Или для разработки с автоперезагрузкой:
```bash
npm run dev
```

6. **Откройте в браузере:**
```
http://localhost:3000
```

**База данных SQLite создается автоматически при первом запуске!**

## Тестовые аккаунты

После выполнения `npm run create-mocks` будут созданы:

- **Грузоотправитель**: `shipper@test.kz` / `test123`
- **Водитель**: `driver@test.kz` / `test123`
- **Администратор**: `admin@logistics.com` / `admin123`

## Использование

### Регистрация

1. Перейдите на страницу регистрации
2. Выберите роль (Грузоотправитель или Водитель)
3. Заполните форму регистрации
4. После регистрации активируйте аккаунт (кнопка "Активировать аккаунт")
5. После активации аккаунт готов к использованию

### Для грузоотправителей

1. После активации создайте заявку на перевозку груза
2. Укажите все необходимые данные:
   - Тип груза (Контейнер, Паллеты и т.д.)
   - Тип машины (Открытый/Закрытый)
   - Вес в кг и тоннах
   - Объем
   - Стоимость в тенге
   - Города отправления и доставки
   - Комментарий
3. Просматривайте свои грузы в разделе "Мои грузы"
4. Отслеживайте статус доставки

### Для водителей

1. После активации заполните профиль водителя
2. Ожидайте подтверждения профиля администратором
3. После подтверждения просматривайте доступные грузы
4. Принимайте заказы

### Для администраторов

1. Войдите с учетными данными: `admin@logistics.com` / `admin123`
2. Перейдите в админ-панель
3. Подтверждайте профили водителей
4. Просматривайте статистику платформы

## Структура проекта

```
logistics/
│
├── server/                          # Backend сервер
│   ├── index.js                     # Главный файл сервера (Express + статика)
│   │
│   ├── config/                      # Конфигурация
│   │   └── database.js              # Настройка подключения к SQLite
│   │
│   ├── models/                      # Модели данных (Sequelize ORM)
│   │   ├── index.js                 # Инициализация моделей и связей
│   │   ├── User.js                  # Модель пользователя
│   │   ├── Cargo.js                 # Модель груза
│   │   └── Driver.js                # Модель водителя
│   │
│   ├── routes/                      # API маршруты (REST API)
│   │   ├── auth.js                  # Аутентификация (регистрация, вход)
│   │   ├── cargo.js                 # Управление грузами
│   │   ├── driver.js                # Профиль водителя и заказы
│   │   ├── payment.js               # Активация аккаунта
│   │   └── admin.js                 # Админ-панель
│   │
│   ├── middleware/                  # Middleware
│   │   └── auth.js                  # JWT аутентификация и проверка оплаты
│   │
│   └── scripts/                     # Утилиты
│       ├── createAdmin.js           # Создание администратора
│       └── createMocks.js           # Создание тестовых данных
│
├── public/                          # Frontend (SPA)
│   ├── index.html                   # Главная HTML страница
│   └── app.js                       # JavaScript приложение (Vanilla JS)
│
├── database.sqlite                  # База данных SQLite (создается автоматически)
│
├── .env                             # Переменные окружения (не в git)
├── env.example                      # Пример файла окружения
├── .gitignore                       # Игнорируемые файлы Git
│
├── package.json                     # Зависимости и скрипты npm
├── package-lock.json                # Заблокированные версии зависимостей
│
├── README.md                        # Документация проекта
└── VPS_DEPLOYMENT.md                # Руководство по развертыванию на VPS
```

### Описание основных директорий

- **`server/`** - Backend код приложения
  - `index.js` - точка входа, настройка Express сервера и маршрутизация
  - `config/` - конфигурационные файлы (база данных, настройки)
  - `models/` - модели данных Sequelize (определение таблиц и связей)
  - `routes/` - обработчики API endpoints (REST API)
  - `middleware/` - промежуточное ПО (аутентификация, валидация)
  - `scripts/` - утилиты для инициализации (создание админа, мокапы)

- **`public/`** - Frontend код (статичные файлы)
  - `index.html` - HTML структура одностраничного приложения
  - `app.js` - весь JavaScript код фронтенда (без фреймворков)

- **Корень проекта**
  - `database.sqlite` - файл базы данных (создается автоматически)
  - `.env` - переменные окружения (секреты, настройки)
  - `package.json` - зависимости Node.js и npm скрипты

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Грузы
- `POST /api/cargo` - Создать груз
- `GET /api/cargo/my` - Мои грузы
- `GET /api/cargo/available` - Доступные грузы

### Водитель
- `POST /api/driver/profile` - Создать/обновить профиль
- `GET /api/driver/profile` - Получить профиль
- `POST /api/driver/accept-order/:cargoId` - Принять заказ

### Платежи (Активация)
- `POST /api/payment/activate` - Активировать аккаунт
- `GET /api/payment/status` - Статус активации

### Админ
- `GET /api/admin/drivers` - Список водителей
- `POST /api/admin/verify-driver/:driverId` - Подтвердить водителя
- `GET /api/admin/stats` - Статистика

## Преимущества

✅ **Один сервер** - все работает из одной команды  
✅ **SQLite** - база данных в файле, без настройки  
✅ **Простота** - нет сложных зависимостей  
✅ **Быстрый старт** - `npm start` и готово  
✅ **Мокапы** - тестовые данные для демонстрации  

## Лицензия

ISC