# 🔄 Инструкция по обновлению проекта на VPS (hoster.kz, Ubuntu 22.04)

Это руководство поможет вам обновить проект на продакшн сервере с последними изменениями из GitHub.

## 📋 Предварительные требования

- Доступ по SSH к серверу
- Проект уже развернут на сервере
- Git настроен на сервере

---

## 🚀 Быстрое обновление (если проект уже настроен)

### 1. Подключитесь к серверу

```bash
ssh root@your_server_ip
# или
ssh username@your_server_ip
```

### 2. Перейдите в директорию проекта

```bash
cd /var/www/logistics
# или в вашу директорию проекта
```

### 3. Остановите приложение

```bash
pm2 stop logistics-platform
# или если используете другой процесс-менеджер:
# systemctl stop logistics-platform
```

### 4. Создайте резервную копию базы данных

```bash
# Создайте директорию для бэкапов (если еще нет)
mkdir -p backups

# Создайте резервную копию
cp database.sqlite backups/database_$(date +%Y%m%d_%H%M%S).sqlite

# Проверьте, что копия создана
ls -lh backups/
```

### 5. Получите последние изменения из GitHub

```bash
# Проверьте текущий статус
git status

# Получите последние изменения
git fetch origin

# Посмотрите, что изменилось (опционально)
git log HEAD..origin/main --oneline

# Обновите код
git pull origin main
```

**Если возникли конфликты:**
```bash
# Сохраните ваши локальные изменения
git stash

# Получите обновления
git pull origin main

# Примените ваши изменения обратно (если нужно)
git stash pop
```

### 6. Установите новые зависимости

```bash
# Установите новые зависимости (если есть)
npm install --production

# Если удалили зависимости, очистите node_modules
# npm prune --production
```

### 7. Выполните миграцию базы данных (если нужно)

```bash
# Запустите миграцию для удаления Firebase полей
npm run migrate
```

Миграция автоматически:
- Создаст резервную копию базы данных
- Удалит колонки `firebaseUid` и `authProvider` из таблицы `users`
- Обновит структуру таблицы

### 8. Обновите файл .env (если нужно)

```bash
# Проверьте, есть ли новые переменные в env.example
diff env.example .env

# Если нужно, обновите .env
nano .env
```

**Важно:** Убедитесь, что в `.env` есть настройки SMTP:
```env
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@shiply.kz
SMTP_PASS=your-password
SMTP_FROM=noreply@shiply.kz
```

### 9. Перезапустите приложение

```bash
# Перезапустите через PM2
pm2 restart logistics-platform

# Проверьте статус
pm2 status

# Проверьте логи
pm2 logs logistics-platform --lines 50
```

### 10. Проверьте работу приложения

```bash
# Проверьте, что приложение запущено
curl http://localhost:3000

# Или откройте в браузере
# http://your_domain.com
```

---

## 📝 Полная инструкция по обновлению

### Шаг 1: Подготовка

```bash
# Подключитесь к серверу
ssh root@your_server_ip

# Перейдите в директорию проекта
cd /var/www/logistics

# Проверьте текущую версию
git log -1 --oneline
```

### Шаг 2: Резервное копирование

```bash
# Создайте директорию для бэкапов
mkdir -p backups

# Создайте резервную копию базы данных
cp database.sqlite backups/database_$(date +%Y%m%d_%H%M%S).sqlite

# Создайте резервную копию .env файла
cp .env backups/.env_$(date +%Y%m%d_%H%M%S)

# Проверьте размер бэкапов
du -sh backups/
```

### Шаг 3: Остановка приложения

```bash
# Остановите приложение
pm2 stop logistics-platform

# Убедитесь, что приложение остановлено
pm2 status
```

### Шаг 4: Обновление кода

```bash
# Проверьте текущую ветку
git branch

# Получите последние изменения
git fetch origin

# Посмотрите список изменений
git log HEAD..origin/main --oneline

# Обновите код
git pull origin main
```

**Если Git не настроен или проект не в репозитории:**

```bash
# Клонируйте репозиторий в новую директорию
cd /var/www
git clone https://github.com/your-username/logistics-platform.git logistics-new

# Скопируйте .env и базу данных
cp logistics/.env logistics-new/
cp logistics/database.sqlite logistics-new/

# Переименуйте директории
mv logistics logistics-old
mv logistics-new logistics

# Перезапустите приложение
cd logistics
pm2 restart logistics-platform
```

### Шаг 5: Установка зависимостей

```bash
# Установите новые зависимости
npm install --production

# Если нужно удалить старые зависимости
npm prune --production
```

### Шаг 6: Миграция базы данных

```bash
# Запустите миграцию
npm run migrate
```

**Что делает миграция:**
- Проверяет наличие колонок `firebaseUid` и `authProvider`
- Создает резервную копию базы данных
- Создает новую таблицу без этих колонок
- Копирует все данные
- Заменяет старую таблицу новой

### Шаг 7: Обновление конфигурации

```bash
# Проверьте, есть ли новые переменные в env.example
cat env.example

# Сравните с текущим .env
diff env.example .env

# Если нужно, обновите .env
nano .env
```

**Обязательно проверьте настройки SMTP:**
```env
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@shiply.kz
SMTP_PASS=your-password
SMTP_FROM=noreply@shiply.kz
```

### Шаг 8: Перезапуск приложения

```bash
# Перезапустите приложение
pm2 restart logistics-platform

# Проверьте статус
pm2 status

# Проверьте логи на ошибки
pm2 logs logistics-platform --lines 100 | grep -i error
```

### Шаг 9: Проверка работы

```bash
# Проверьте, что приложение отвечает
curl -I http://localhost:3000

# Проверьте логи
pm2 logs logistics-platform --lines 20
```

**Проверьте в браузере:**
- Откройте `http://your_domain.com` или `https://your_domain.com`
- Попробуйте зарегистрироваться
- Проверьте отправку OTP кода

---

## 🔧 Решение проблем

### Ошибка "Cannot find module"

```bash
# Переустановите зависимости
rm -rf node_modules
npm install --production
```

### Ошибка миграции базы данных

```bash
# Восстановите из резервной копии
cp backups/database_YYYYMMDD_HHMMSS.sqlite database.sqlite

# Попробуйте миграцию снова
npm run migrate
```

### Приложение не запускается

```bash
# Проверьте логи
pm2 logs logistics-platform --lines 50

# Проверьте переменные окружения
cat .env

# Проверьте порт
netstat -tulpn | grep 3000
```

### Ошибка 500 на продакшене

```bash
# Проверьте логи приложения
pm2 logs logistics-platform

# Проверьте структуру базы данных
sqlite3 database.sqlite "PRAGMA table_info(users);"

# Если есть старые колонки, запустите миграцию
npm run migrate
```

### Git конфликты

```bash
# Сохраните локальные изменения
git stash

# Получите обновления
git pull origin main

# Примените изменения обратно
git stash pop

# Если есть конфликты, разрешите их вручную
nano conflicted_file.js
```

---

## 📋 Чек-лист обновления

- [ ] Подключился к серверу
- [ ] Создал резервную копию базы данных
- [ ] Создал резервную копию .env файла
- [ ] Остановил приложение (pm2 stop)
- [ ] Получил последние изменения (git pull)
- [ ] Установил новые зависимости (npm install)
- [ ] Выполнил миграцию базы данных (npm run migrate)
- [ ] Обновил .env файл (если нужно)
- [ ] Перезапустил приложение (pm2 restart)
- [ ] Проверил логи на ошибки
- [ ] Проверил работу в браузере

---

## 🚨 Важные замечания

1. **Всегда делайте резервную копию** перед обновлением
2. **Проверяйте логи** после перезапуска
3. **Тестируйте на локальной машине** перед обновлением продакшена
4. **Обновляйте в нерабочее время** для минимизации простоев
5. **Имейте план отката** на случай проблем

---

## 🔄 Автоматическое обновление (опционально)

Можно создать скрипт для автоматического обновления:

```bash
# Создайте файл update.sh
nano /var/www/logistics/update.sh
```

```bash
#!/bin/bash

cd /var/www/logistics

# Резервная копия
mkdir -p backups
cp database.sqlite backups/database_$(date +%Y%m%d_%H%M%S).sqlite

# Остановка приложения
pm2 stop logistics-platform

# Обновление кода
git pull origin main

# Установка зависимостей
npm install --production

# Миграция
npm run migrate

# Перезапуск
pm2 restart logistics-platform

# Проверка
pm2 logs logistics-platform --lines 20
```

Сделайте скрипт исполняемым:
```bash
chmod +x /var/www/logistics/update.sh
```

Запуск:
```bash
/var/www/logistics/update.sh
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs logistics-platform`
2. Проверьте резервную копию: `ls -lh backups/`
3. Восстановите из резервной копии при необходимости

---

**Готово!** 🎉 Проект обновлен и готов к работе.
