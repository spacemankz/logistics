# 🚀 Руководство по развертыванию на VPS сервере

Это подробное руководство поможет вам развернуть логистическую платформу на VPS сервере.

## 📋 Содержание

1. [Требования](#требования)
2. [Подготовка VPS сервера](#подготовка-vps-сервера)
3. [Установка Node.js](#установка-nodejs)
4. [Развертывание приложения](#развертывание-приложения)
5. [Настройка окружения](#настройка-окружения)
6. [Запуск приложения](#запуск-приложения)
7. [Настройка PM2 (процесс-менеджер)](#настройка-pm2-процесс-менеджер)
8. [Настройка Nginx (опционально)](#настройка-nginx-опционально)
9. [Настройка SSL сертификата (Let's Encrypt)](#настройка-ssl-сертификата-lets-encrypt)
10. [Настройка Firewall](#настройка-firewall)
11. [Полезные команды](#полезные-команды)
12. [Решение проблем](#решение-проблем)

---

## Требования

- VPS сервер с Ubuntu 20.04/22.04 или Debian 11/12
- Доступ по SSH с правами root или sudo
- Минимум 1GB RAM, 10GB дискового пространства
- Доменное имя (опционально, для SSL)

## Особенности приложения

- **Одностраничное приложение (SPA)** - весь фронтенд и бэкенд в одном проекте
- **SQLite база данных** - не требует установки PostgreSQL или MySQL, работает из коробки
- **Простое развертывание** - один сервер, один процесс, все работает сразу

---

## Подготовка VPS сервера

### 1. Подключение к серверу

```bash
ssh root@your_server_ip
# или
ssh username@your_server_ip
```

### 2. Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### 3. Установка базовых инструментов

```bash
sudo apt install -y curl wget git build-essential
```

---

## Установка Node.js

### Вариант 1: Установка через NodeSource (рекомендуется)

```bash
# Для Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка версии
node --version
npm --version
```

### Вариант 2: Установка через NVM (Node Version Manager)

```bash
# Установка NVM
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезагрузка терминала или выполнение:
source ~/.bashrc

# Установка Node.js
nvm install 20
nvm use 20
nvm alias default 20

# Проверка
node --version
npm --version
```

---

## Развертывание приложения

### 1. Создание директории для приложения

```bash
# Создаем директорию
sudo mkdir -p /var/www/logistics
sudo chown -R $USER:$USER /var/www/logistics
cd /var/www/logistics
```

### 2. Загрузка проекта

#### Вариант A: Через Git (если проект в репозитории)

```bash
git clone https://github.com/your-username/logistics-platform.git .
```

#### Вариант B: Через SCP (с локального компьютера)

На вашем локальном компьютере:

```bash
# Создайте архив проекта
tar -czf logistics.tar.gz --exclude='node_modules' --exclude='database.sqlite' --exclude='.git' logistics/

# Загрузите на сервер
scp logistics.tar.gz root@your_server_ip:/var/www/logistics/
```

На сервере:

```bash
cd /var/www/logistics
tar -xzf logistics.tar.gz --strip-components=1
rm logistics.tar.gz
```

#### Вариант C: Через rsync

```bash
rsync -avz --exclude 'node_modules' --exclude 'database.sqlite' --exclude '.git' ./ root@your_server_ip:/var/www/logistics/
```

### 3. Установка зависимостей

```bash
cd /var/www/logistics
npm install --production
```

---

## Настройка окружения

### 1. Создание файла .env

```bash
cd /var/www/logistics
cp env.example .env
nano .env
```

### 2. Настройка переменных окружения

Отредактируйте `.env` файл:

```env
# Порт приложения
PORT=3000

# Окружение
NODE_ENV=production

# SQLite Database (файл создается автоматически)
DB_PATH=/var/www/logistics/database.sqlite

# JWT секретный ключ (сгенерируйте случайную строку)
JWT_SECRET=your_super_secret_jwt_key_change_this_to_random_string
JWT_EXPIRE=7d

# Регистрация
REGISTRATION_FEE=1000

# Admin (данные администратора создаются через npm run create-admin)
ADMIN_EMAIL=admin@logistics.com
ADMIN_PASSWORD=admin123
```

**Важно:** Сгенерируйте безопасный JWT_SECRET:

```bash
# Генерация случайного ключа
openssl rand -base64 32
```

Скопируйте сгенерированный ключ и вставьте в `JWT_SECRET` в файле `.env`.

### 3. Создание директории для базы данных

```bash
# База данных будет создана автоматически при первом запуске
# Но можно создать директорию заранее
mkdir -p /var/www/logistics
chmod 755 /var/www/logistics
```

---

## Запуск приложения

### 1. Создание администратора

```bash
cd /var/www/logistics
npm run create-admin
```

Скрипт создаст администратора с данными из `.env` файла (ADMIN_EMAIL и ADMIN_PASSWORD).
Если нужно создать администратора с другими данными, отредактируйте `.env` перед запуском скрипта.

### 2. Создание тестовых данных (опционально)

```bash
npm run create-mocks
```

### 3. Тестовый запуск

```bash
npm start
```

Проверьте, что приложение работает, открыв в браузере: `http://your_server_ip:3000`

Остановите приложение (Ctrl+C) и переходите к настройке PM2.

---

## Настройка PM2 (процесс-менеджер)

PM2 обеспечит автоматический перезапуск приложения при сбоях и после перезагрузки сервера.

### 1. Установка PM2

```bash
sudo npm install -g pm2
```

### 2. Запуск приложения через PM2

```bash
cd /var/www/logistics
pm2 start server/index.js --name logistics-platform
```

Или используйте npm скрипт:

```bash
cd /var/www/logistics
pm2 start npm --name logistics-platform -- start
```

### 3. Сохранение конфигурации PM2

```bash
# Сохранить текущий список процессов
pm2 save

# Настроить автозапуск при перезагрузке системы
pm2 startup
# Выполните команду, которую выведет PM2 (обычно что-то вроде):
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u $USER --hp /home/$USER
```

### 4. Полезные команды PM2

```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs logistics-platform

# Перезапуск
pm2 restart logistics-platform

# Остановка
pm2 stop logistics-platform

# Удаление из PM2
pm2 delete logistics-platform

# Мониторинг
pm2 monit
```

---

## Настройка Nginx (опционально)

Nginx будет работать как reverse proxy, перенаправляя запросы на ваше Node.js приложение.

### 1. Установка Nginx

```bash
sudo apt install -y nginx
```

### 2. Создание конфигурации

```bash
sudo nano /etc/nginx/sites-available/logistics
```

Добавьте следующую конфигурацию:

```nginx
server {
    listen 80;
    server_name your_domain.com www.your_domain.com;

    # Если нет домена, используйте IP
    # server_name _;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты для больших файлов
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Увеличение размера загружаемых файлов (если нужно)
    client_max_body_size 10M;
}
```

### 3. Активация конфигурации

```bash
# Создать символическую ссылку
sudo ln -s /etc/nginx/sites-available/logistics /etc/nginx/sites-enabled/

# Удалить дефолтную конфигурацию (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверить конфигурацию
sudo nginx -t

# Перезапустить Nginx
sudo systemctl restart nginx

# Включить автозапуск
sudo systemctl enable nginx
```

### 4. Проверка работы

Откройте в браузере: `http://your_domain.com` или `http://your_server_ip`

---

## Настройка SSL сертификата (Let's Encrypt)

Для безопасного HTTPS соединения используйте бесплатный SSL сертификат от Let's Encrypt.

### 1. Установка Certbot

```bash
sudo apt install -y certbot python3-certbot-nginx
```

### 2. Получение SSL сертификата

```bash
sudo certbot --nginx -d your_domain.com -d www.your_domain.com
```

Следуйте инструкциям:
- Введите email
- Согласитесь с условиями
- Выберите, перенаправлять ли HTTP на HTTPS (рекомендуется: 2)

### 3. Автоматическое обновление сертификата

Certbot автоматически настроит cron-задачу для обновления сертификата. Проверить можно:

```bash
sudo certbot renew --dry-run
```

### 4. Обновление конфигурации Nginx

Certbot автоматически обновит конфигурацию Nginx для работы с HTTPS.

---

## Настройка Firewall

### 1. Настройка UFW (Uncomplicated Firewall)

```bash
# Разрешить SSH (важно сделать первым!)
sudo ufw allow OpenSSH

# Разрешить HTTP и HTTPS
sudo ufw allow 'Nginx Full'
# или отдельно:
# sudo ufw allow 80/tcp
# sudo ufw allow 443/tcp

# Если не используете Nginx, разрешите прямой доступ к приложению
# sudo ufw allow 3000/tcp

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status
```

### 2. Настройка Cloud Firewall (если используете облачный провайдер)

Если вы используете DigitalOcean, AWS, или другой облачный провайдер, настройте firewall в их панели управления:

- **Входящие правила:**
  - SSH (22/tcp)
  - HTTP (80/tcp)
  - HTTPS (443/tcp)
  - Приложение (3000/tcp) - только если не используете Nginx

- **Исходящие правила:**
  - Разрешить все (по умолчанию)

---

## Полезные команды

### Мониторинг приложения

```bash
# Логи PM2
pm2 logs logistics-platform

# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Использование ресурсов
htop
# или
top

# Использование диска
df -h

# Использование памяти
free -h
```

### Обновление приложения

```bash
cd /var/www/logistics

# Остановить приложение
pm2 stop logistics-platform

# Сделать backup базы данных
mkdir -p backups
cp database.sqlite backups/database_$(date +%Y%m%d_%H%M%S).sqlite

# Обновить код (если через Git)
git pull origin main

# Или загрузить новый код через SCP/rsync (см. раздел "Развертывание приложения")

# Установить новые зависимости
npm install --production

# Перезапустить приложение
pm2 restart logistics-platform

# Проверить статус
pm2 status

# Проверить логи
pm2 logs logistics-platform --lines 50
```

### Резервное копирование базы данных

```bash
# Создать backup
cp /var/www/logistics/database.sqlite /var/www/logistics/backups/database_$(date +%Y%m%d_%H%M%S).sqlite

# Автоматический backup через cron
# Добавьте в crontab (crontab -e):
# 0 2 * * * cp /var/www/logistics/database.sqlite /var/www/logistics/backups/database_$(date +\%Y\%m\%d_\%H\%M\%S).sqlite
```

---

## Решение проблем

### Приложение не запускается

1. **Проверьте логи:**
   ```bash
   pm2 logs logistics-platform
   ```

2. **Проверьте порт:**
   ```bash
   sudo netstat -tulpn | grep 3000
   # или
   sudo ss -tulpn | grep 3000
   ```

3. **Проверьте права доступа:**
   ```bash
   ls -la /var/www/logistics
   sudo chown -R $USER:$USER /var/www/logistics
   ```

### Ошибка "EADDRINUSE"

Порт уже занят другим процессом:

```bash
# Найти процесс
sudo lsof -i :3000
# или
sudo fuser -k 3000/tcp

# Убить процесс
sudo kill -9 <PID>
```

### База данных не создается

```bash
# Проверьте права на директорию
ls -la /var/www/logistics
sudo chmod 755 /var/www/logistics
sudo chown -R $USER:$USER /var/www/logistics
```

### Nginx возвращает 502 Bad Gateway

1. **Проверьте, что приложение запущено:**
   ```bash
   pm2 status
   ```

2. **Проверьте логи Nginx:**
   ```bash
   sudo tail -f /var/log/nginx/error.log
   ```

3. **Проверьте конфигурацию Nginx:**
   ```bash
   sudo nginx -t
   ```

### Проблемы с SSL сертификатом

```bash
# Проверить статус сертификата
sudo certbot certificates

# Обновить сертификат вручную
sudo certbot renew

# Проверить конфигурацию Nginx
sudo nginx -t
sudo systemctl reload nginx
```

### Высокое использование памяти

```bash
# Перезапустить приложение
pm2 restart logistics-platform

# Очистить кэш PM2
pm2 flush

# Проверить использование памяти
pm2 monit
```

---

## Безопасность

### Рекомендации по безопасности

1. **Измените SSH порт:**
   ```bash
   sudo nano /etc/ssh/sshd_config
   # Измените Port 22 на другой порт
   sudo systemctl restart sshd
   ```

2. **Отключите вход root по SSH:**
   ```bash
   sudo nano /etc/ssh/sshd_config
   # Установите PermitRootLogin no
   sudo systemctl restart sshd
   ```

3. **Используйте ключи SSH вместо паролей**

4. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

5. **Настройте fail2ban для защиты от брутфорса:**
   ```bash
   sudo apt install -y fail2ban
   sudo systemctl enable fail2ban
   sudo systemctl start fail2ban
   ```

6. **Используйте сильные пароли для JWT_SECRET**

7. **Регулярно делайте резервные копии базы данных**

---

## Дополнительные настройки

### Настройка автоматического перезапуска при сбое

PM2 уже настроен на автоматический перезапуск, но можно добавить дополнительные опции:

```bash
pm2 start server/index.js --name logistics-platform --max-memory-restart 500M --exp-backoff-restart-delay=100
pm2 save
```

### Настройка логирования

Создайте директорию для логов:

```bash
mkdir -p /var/www/logistics/logs
```

Настройте ротацию логов в PM2:

```bash
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7
```

---

## Поддержка

Если у вас возникли проблемы:

1. Проверьте логи приложения: `pm2 logs logistics-platform`
2. Проверьте логи Nginx: `sudo tail -f /var/log/nginx/error.log`
3. Проверьте системные логи: `sudo journalctl -u nginx -f`
4. Убедитесь, что все порты открыты в firewall
5. Проверьте, что домен правильно указывает на IP сервера (если используете домен)

---

## Чек-лист развертывания

- [ ] VPS сервер подготовлен и обновлен
- [ ] Node.js установлен (версия 14+)
- [ ] Проект загружен на сервер
- [ ] Зависимости установлены (`npm install --production`)
- [ ] Файл `.env` создан и настроен (особенно JWT_SECRET!)
- [ ] Администратор создан (`npm run create-admin`)
- [ ] Тестовые данные созданы (`npm run create-mocks`) - опционально
- [ ] Приложение протестировано локально (`npm start`)
- [ ] Приложение запущено через PM2
- [ ] PM2 настроен на автозапуск (`pm2 startup` и `pm2 save`)
- [ ] Nginx установлен и настроен (опционально, но рекомендуется)
- [ ] SSL сертификат установлен (опционально, но рекомендуется для продакшена)
- [ ] Firewall настроен (UFW)
- [ ] Резервное копирование базы данных настроено
- [ ] Приложение доступно через браузер

---

## Быстрая справка

### Структура проекта
```
/var/www/logistics/
├── server/          # Backend (Express + API)
├── public/          # Frontend (HTML, CSS, JS - SPA)
├── database.sqlite  # База данных SQLite
├── .env            # Конфигурация
└── package.json    # Зависимости
```

### Основные команды

```bash
# Запуск приложения
pm2 start logistics-platform
pm2 restart logistics-platform
pm2 stop logistics-platform

# Просмотр логов
pm2 logs logistics-platform

# Статус
pm2 status

# Создание администратора
npm run create-admin

# Создание тестовых данных
npm run create-mocks
```

### Тестовые аккаунты (после create-mocks)

- **Грузоотправитель:** shipper@test.kz / test123
- **Водитель:** driver@test.kz / test123  
- **Администратор:** admin@logistics.com / admin123

---

**Готово!** 🎉 Ваше приложение должно быть доступно по адресу вашего сервера (или доменному имени, если настроено).

Для проверки откройте в браузере:
- `http://your_server_ip` (если Nginx настроен)
- `http://your_server_ip:3000` (если без Nginx)
- `https://your_domain.com` (если SSL настроен)





